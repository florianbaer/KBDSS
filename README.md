# Word tokenization and stemming and lemmatization

First run
´´´
virutalenv kbdss -p python3.7
source kbdss/bin/activate
pip install -U nltk
pip install -U numpy
´´´

Then run the given script with python [FILENAME HERE]
